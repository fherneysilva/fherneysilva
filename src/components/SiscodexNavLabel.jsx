import React from "react";
import "../styles/SiscodexNavLabel.css";

const SiscodexNavLabel = ({ text }) => (
  <span className="siscodex-nav-label">{text}</span>
);

export default SiscodexNavLabel;
